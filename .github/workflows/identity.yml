name: Identity

on:
  pull_request:
  push:
    branches: [main]

jobs:
  canonical-name:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject legacy product names
        run: sh .github/scripts/check-identity.sh
