# bd#69 reviewer dry-run + release verification — 2026-08-31

Release cut: branch `release/1.0.3` (marketing version 1.0), gate flip
committed (`STORE_SURFACE_GATED = YES`), archived with no CLI overrides;
archive Info.plist verified `BDStoreSurfaceGated = YES`; uploaded via the
free local cloud-signing rail ("Upload succeeded"). Tag `store/1.0-buildN`
lands once ASC assigns the build number.

| Check | Device | Result |
|---|---|---|
| ReviewerSimulationTests (full first-ten-minutes + Cupertino baseline, LIVE production engine) | iPhone 17 Pro Max, Release | PASS |
| ReviewerSimulationTests | iPad Pro 13-inch (M5), Release | PASS |
| StoreSurfaceGateUITests — 3 gated-surface checks | both devices | PASS |
| testUngatedBuildKeepsAllSurfaces | — | main-branch-only by design (asserts the surface the release cut removes); passes on main |

Engine health at run time: 2,783 venues, 50/50 baseline shards, 2,144 venues with photos.
